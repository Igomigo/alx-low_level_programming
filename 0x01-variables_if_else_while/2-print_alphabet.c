#include <stdio.h>

/**
 * main - A program that prints all the alphabet in lowercase
 *
 * Return: 0
 */

int main(void)
{

	char letter;
	
	for(letter = a; a <= z; letter++)
		putchar(letter);

	putchar(\n);
	return = 0;
}
